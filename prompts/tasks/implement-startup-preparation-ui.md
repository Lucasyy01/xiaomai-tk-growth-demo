基于你已经确认过的改造方案，现在开始实施。

目标：
将“启动前检查 / 账户与扣款 / 授信审批”整合为一个“启动准备”页面，内部通过 3 个 Tab 承载，并让页面更像成熟企业 SaaS 的阶段页，而不是 3 个零散子页或 3 个复杂后台页拼接体。

本轮强约束：
1. 保留当前页面核心业务含义
2. 当前阶段不深改业务逻辑
3. 优先使用 antd
4. “启动准备”应作为一个阶段页成立，而不是 3 个散页入口
5. 页面头部只出现一次，并统一承接：
   - 当前客户
   - 当前总体状态
   - 总体进度
   - 返回 CustomerAdmission 的入口
6. 页面必须通过 3 个 Tab 承载：
   - 启动前检查
   - 账户与扣款
   - 授信审批
7. 每个 Tab 必须保持轻量，只保留：
   - 当前 Tab 结论
   - 分项明细
   - 当前选中项说明
   - 当前唯一主动作
8. 不允许把 3 个复杂后台页硬塞在同一个页面
9. 不允许做多个同等级主按钮
10. 不允许新增流程说明区、培训说明区、大量解释模块
11. 尽量减少对旧 styles.css 的依赖
12. 默认只修改与启动准备整合直接相关的文件
13. 不擅自修改无关页面和导航逻辑

建议实现目标：
- 让原本分散的 LaunchChecklist / PaymentAuthorization / CreditApproval 在体验层合并为一个“启动准备”页面
- 旧页面如需保留，可作为过渡或内部组件来源，但不要继续作为主要体验入口

输出要求：

## 1. 完整代码
请输出你实际修改的所有相关文件完整代码

## 2. 改动说明
请列出：
- 改了哪些文件
- 启动准备页的整体结构是什么
- 3 个 Tab 分别承载什么
- 为什么这比 3 个零散子页更合理
- 是否保留了原组件、如何处理旧入口

## 3. 自我检查
请补充说明：
- 这版是否仍然只保留了清晰的当前主动作
- 这版是否避免了页面过重
- 这版是否仍然符合“总 → 分”结构